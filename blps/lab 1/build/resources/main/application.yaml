spring:
  application:
    name: blps-lab
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: "postgres"
    password: "postgres"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

app:
  security:
    jwt:
      secret: ${JWT_SECRET:zmNVBjHVFjhWVFJVFEGveJFV<MFen,nbaewfkchgfFEFWFw}
      expiration-hours: ${JWT_EXPIRATION_HOURS:24}
    cors:
      allowed-origins:
        - "http://localhost:3000"
        - "http://localhost:8080"
        - "http://127.0.0.1:3000"
